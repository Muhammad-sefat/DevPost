// Dashboard validation schemas (e.g. date range filters, report generation)
// import { z } from "zod";

// export const dashboardFilterSchema = z.object({
//   dateRange: z.enum(["7d", "30d", "90d", "1y"]).default("30d"),
// });
